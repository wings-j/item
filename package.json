{
  "name": "@wings-j/item",
  "version": "0.0.0",
  "description": "Item with name, value and children (like a tree). Especially useful for selections whose text for the users are different with the data for programmes.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules npx jest",
    "release": "npm publish --access=public"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "typescript": "^5.3.3"
  }
}
